# Secrets
.env
.env.*

# Python
.venv/
vllm_env/
__pycache__/
*.pyc
*.pyo

# Pipeline artefacts (regenerated from .nsys-rep)
out/
# Theoretical-comparison outputs (per-model, regenerated by compare_profile.sh).
# Aggregate_scan.py stays tracked; per-model output subdirs are gitignored.
reports/*/

# Profiling reports (large nsys binaries; users supply their own)
profile/results/
*.nsys-rep
*.sqlite
*.qdrep
